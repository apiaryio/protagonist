machine:
  node:
    version: 0.10.36

checkout:
  post:
    - git submodule update --init --recursive

deployment:
  release:
    tag: /v.*/
    commands:
      - echo '//registry.npmjs.org/:_authToken=${NPM_TOKEN}' >.npmrc
      - npm publish
